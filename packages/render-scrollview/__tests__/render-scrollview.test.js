"use strict";

const renderScrollview = require("..");
const assert = require("assert").strict;

assert.strictEqual(renderScrollview(), "Hello from renderScrollview");
console.info("renderScrollview tests passed");
